import React from 'react';
import Card from '@material-ui/core/Card';
import CardContent from '@material-ui/core/CardContent';

interface Props {}

const StaffNew: React.FC<Props> = () => {
  return (
    <Card>
      <CardContent>new</CardContent>
    </Card>
  );
};

export default StaffNew;
